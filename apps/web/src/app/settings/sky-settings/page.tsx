export default function SettingsSkySettingsPage() {
  return (
    <main style={{ display: "flex", flexDirection: "column", gap: 16 }}>
      <h2>Sky Settings</h2>
      <p style={{ maxWidth: 640, color: "var(--text-secondary)" }}>
        Configure global environmental presets such as skyboxes, ambient colors, and lighting defaults.
      </p>
    </main>
  );
}


